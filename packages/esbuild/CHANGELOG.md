# @mochi-css/esbuild

## 7.1.0

### Minor Changes

- e9b228d: Add `@mochi-css/esbuild` plugin for standalone library builds. Supports virtual CSS mode (default) and external CSS mode (`externalCss: true`) for distributing component libraries to Vite/Next.js consumers. Adds a matching `esbuild` preset to `tsuki`.
