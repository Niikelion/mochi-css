// @generated — do not edit; regenerated from mochi-vanilla-react by codegen/index.ts

// eslint-disable-next-line @typescript-eslint/ban-ts-comment
// @ts-ignore — styled-system/ is generated by `panda codegen` before the build
import { cva } from "styled-system/css"
import React from "react"

const _buttonRecipe = cva({
    base: {
        display: "inline-flex",
        alignItems: "center",
        justifyContent: "center",
        fontFamily: "mono",
        fontWeight: "600",
        lineHeight: "normal",
        cursor: "pointer",
        border: "1px solid transparent",
        borderRadius: "sm",
        transition: "opacity 0.15s, transform 0.15s",
        _hover: {
            opacity: 0.85,
        },
    },
    variants: {
        variant: {
            solid: {
                backgroundColor: "var(--btn-accent)",
                color: "var(--btn-on-accent)",
                borderColor: "var(--btn-accent)",
            },
            outline: {
                backgroundColor: "transparent",
                color: "var(--btn-accent)",
                borderColor: "var(--btn-accent)",
            },
            ghost: {
                backgroundColor: "transparent",
                color: "var(--btn-accent)",
                borderColor: "transparent",
            },
        },
        size: {
            sm: {
                padding: "6px 12px",
                fontSize: "xs",
            },
            md: {
                padding: "8px 16px",
                fontSize: "sm",
            },
            lg: {
                padding: "12px 24px",
                fontSize: "base",
            },
        },
        color: {
            gold: {
                "--btn-accent": "var(--colors-gold)",
                "--btn-on-accent": "var(--colors-bg)",
            },
            neutral: {
                "--btn-accent": "var(--colors-text)",
                "--btn-on-accent": "var(--colors-bg)",
            },
            danger: {
                "--btn-accent": "#e54d2e",
                "--btn-on-accent": "#fff",
            },
        },
    },
    defaultVariants: {
        variant: "solid",
        size: "md",
        color: "gold",
    },
})
export const Button = ({
    variant = "solid", size = "md", color = "gold", className, children, ...p
}: {
    variant?: "solid" | "outline" | "ghost",
    size?: "sm" | "md" | "lg",
    color?: "gold" | "neutral" | "danger",
} & React.ComponentPropsWithoutRef<"button">) =>
    <button {...p} className={[_buttonRecipe({ variant, size, color }), className].filter(Boolean).join(" ")}>{children}</button>
