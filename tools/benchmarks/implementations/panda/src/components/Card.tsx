// @generated — do not edit; regenerated from mochi-vanilla-react by codegen/index.ts

// eslint-disable-next-line @typescript-eslint/ban-ts-comment
// @ts-ignore — styled-system/ is generated by `panda codegen` before the build
import { cva } from "styled-system/css"
import React from "react"

const _cardRecipe = cva({
    base: {
        borderRadius: "md",
        padding: "4",
        border: "1px solid",
    },
    variants: {
        variant: {
            default: {
                backgroundColor: "surface",
                borderColor: "border",
            },
            elevated: {
                backgroundColor: "surface",
                borderColor: "border",
                boxShadow: "0 4px 24px rgba(0,0,0,0.4)",
            },
            bordered: {
                backgroundColor: "surface",
                borderColor: "gold",
            },
        },
    },
    defaultVariants: {
        variant: "default",
    },
})
export const Card = ({
    variant = "default", className, children, ...p
}: {
    variant?: "default" | "elevated" | "bordered",
} & React.ComponentPropsWithoutRef<"div">) =>
    <div {...p} className={[_cardRecipe({ variant }), className].filter(Boolean).join(" ")}>{children}</div>
