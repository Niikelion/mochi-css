// @generated — do not edit; regenerated from mochi-vanilla-react by codegen/index.ts

// eslint-disable-next-line @typescript-eslint/ban-ts-comment
// @ts-ignore — styled-system/ is generated by `panda codegen` before the build
import { cva } from "styled-system/css"
import React from "react"

const _frameRecipe = cva({
    base: {
        display: "flex",
        padding: "3",
        gap: "2",
        borderWidth: "1px",
        borderStyle: "solid",
        borderColor: "border",
        borderRadius: "md",
        width: "200px",
        height: "200px",
        backgroundColor: "surface",
    },
    variants: {
        direction: {
            row: {
                flexDirection: "row",
                justifyContent: "var(--alignX)",
                alignItems: "var(--alignY)",
            },
            col: {
                flexDirection: "column",
                justifyContent: "var(--alignY)",
                alignItems: "var(--alignX)",
            },
        },
        alignX: {
            start: {
                ["--alignX"]: "flex-start",
            },
            center: {
                ["--alignX"]: "center",
            },
            end: {
                ["--alignX"]: "flex-end",
            },
        },
        alignY: {
            start: {
                ["--alignY"]: "flex-start",
            },
            center: {
                ["--alignY"]: "center",
            },
            end: {
                ["--alignY"]: "flex-end",
            },
        },
    },
    defaultVariants: {
        direction: "row",
        alignX: "start",
        alignY: "start",
    },
})
export const Frame = ({
    direction = "row", alignX = "start", alignY = "start", className, children, ...p
}: {
    direction?: "row" | "col",
    alignX?: "start" | "center" | "end",
    alignY?: "start" | "center" | "end",
} & React.ComponentPropsWithoutRef<"div">) =>
    <div {...p} className={[_frameRecipe({ direction, alignX, alignY }), className].filter(Boolean).join(" ")}>{children}</div>
