// @generated — do not edit; regenerated from mochi-vanilla-react by codegen/index.ts

// eslint-disable-next-line @typescript-eslint/ban-ts-comment
// @ts-ignore — styled-system/ is generated by `panda codegen` before the build
import { cva } from "styled-system/css"
import { createElement, type HTMLProps } from "react"
import React from "react"

const headingStyles = cva({
    base: {
        fontFamily: "mono",
        lineHeight: "1.2",
        fontWeight: "700",
    },
    variants: {
        size: {
            sm: {
                fontSize: "lg",
            },
            md: {
                fontSize: "2xl",
            },
            lg: {
                fontSize: "3xl",
            },
        },
        color: {
            default: {
                color: "text",
            },
            gold: {
                color: "gold",
            },
            dim: {
                color: "dim",
            },
        },
    },
    defaultVariants: {
        size: "md",
        color: "default",
    },
})

type HeadingProps = {
    as?: "h1" | "h2" | "h3" | "h4"
    size?: "sm" | "md" | "lg"
    color?: "default" | "gold" | "dim"
} & HTMLProps<HTMLHeadingElement>

export const Heading = ({ as: tag = "h2", size, color, className, ...props }: HeadingProps) => {
    const cls = [headingStyles({ size, color }), className].filter(Boolean).join(" ")
    return createElement(tag, { className: cls, ...props })
}
