// @generated — do not edit; regenerated from mochi-vanilla-react by codegen/index.ts

// eslint-disable-next-line @typescript-eslint/ban-ts-comment
// @ts-ignore — styled-system/ is generated by `panda codegen` before the build
import { css } from "styled-system/css"
import React from "react"

const _preCss = css({
        backgroundColor: "bg",
        borderWidth: "1px",
        borderStyle: "solid",
        borderColor: "border",
        borderRadius: "md",
        padding: "4",
        overflow: "auto",
        fontSize: "sm",
        color: "text",
        lineHeight: "1.6",
    })
export const Pre = ({ className, children, ...p }: React.ComponentPropsWithoutRef<"pre">) =>
    <pre {...p} className={[_preCss, className].filter(Boolean).join(" ")}>{children}</pre>
